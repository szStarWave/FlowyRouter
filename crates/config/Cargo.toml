[package]
name = "flowy-config"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cross-platform paths and config file for Flowy Router (~/.flowy-router)"

[dependencies]
anyhow = "1"
dirs = "6"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
